# GitHub-Project-Publisher Skill 作用总结

> 本总结基于 `github-project-publisher` Skill 的完整定义编写，涵盖其定位、适用场景、五阶段发布工作流、确认门控与关键约束，帮助使用者快速理解该 Skill 能做什么、怎么运转。

---

## 一、Skill 定位

**GitHub Project Publisher（GitHub 项目首次发布工作流）** 是一套将**本地项目按标准化流程首次发布到 GitHub** 的流程 Skill。

- **核心作用**：把"本地一个还没进版本控制的项目"变成"GitHub 上一个有 README、已公开、带 v0.1.0 Tag 和 Release 的正式仓库"，全程分阶段、带确认门控，避免漏步骤或误操作。
- **一句话概括**：从 README 完善到首次公开推送，再到打 Tag 发 Release，一条龙规范发布。

---

## 二、适用场景与前置约定

| 维度 | 说明 |
|------|------|
| **触发场景** | 将本地项目从零发布到 GitHub、按规范流程完成首次提交与公开推送、提到"发布到 GitHub""初始化仓库""打 Tag 发 Release"等 |
| **工作目录** | 用户当前项目根目录，所有命令在此执行 |
| **环境约定** | Git 与 GitHub CLI（`gh`）均已安装，无需检查安装状态 |
| **执行原则** | 五个阶段必须按顺序执行；涉及用户确认的阶段，未获确认不得继续 |

---

## 三、五阶段工作流

```
阶段一：完善 README.md
   ↓
阶段二：敏感信息排查（只报告，不执行 Git）
   ↓ 用户确认无问题
阶段三：初始化 Git 仓库并首次提交
   ↓
阶段四：发布到 GitHub（确认仓库名/可见性/操作清单）
   ↓
阶段五：v0.1.0 Tag 与 Release（单独确认是否执行 + 内容确认）
```

### 阶段一：完善 README.md

检查项目根目录是否存在 `README.md`：不存在则创建，存在则补全缺失部分。

README 必须包含以下**五个章节**，且内容必须与项目真实功能一致，**不得虚构**：

1. **项目解决什么问题** — 项目背景与目标
2. **主要功能** — 核心功能点列表
3. **安装方法** — 依赖安装与环境配置步骤
4. **使用方法** — 基本运行/调用方式
5. **输入输出示例** — 至少一个可运行示例，含输入和预期输出

完成后向用户简要汇报 README 已包含的章节。

### 阶段二：敏感信息排查

对整个项目进行扫描，检查是否存在：

- **密码或 Token**：API Key、Secret、Access Token 等
- **个人隐私**：姓名、邮箱、手机号、身份证号等
- **本地绝对路径**：如 `/home/user/...`、`C:\Users\...`
- **临时文件**：`*.tmp`、`*.log`、`*.bak`、`__pycache__/`、`.DS_Store` 等
- **测试垃圾文件**
- **其他不应该公开的内容**

**硬约束**：本阶段**只向用户报告检查结果，绝不执行任何 Git 操作**（不 init、不 add、不 commit）。

报告格式：逐项列出发现的问题及文件路径；若无问题，明确说明"未发现敏感信息"。发现问题时建议用户处理后再进入下一阶段。

### 阶段三：初始化 Git 仓库并首次提交

在用户确认阶段二无问题（或问题已处理）后执行：

1. 初始化 Git 仓库：`git init`
2. 将默认分支设为 `main`：`git branch -M main`
3. 添加所有文件：`git add .`
4. 首次提交：`git commit -m "Initial commit"`

提交完成后汇报提交哈希与文件数量。

### 阶段四：发布到 GitHub

**执行前必须让用户确认以下三项，未确认不得执行：**

1. **仓库名称** — 默认使用项目目录名，用户可手动修改
2. **可见性** — 固定为 `Public`（公开）
3. **即将执行的操作** — 明确列出将创建远程仓库、添加 remote、推送 main 分支

确认后依次执行：

1. 创建 GitHub 远程仓库（Public）：
   ```bash
   gh repo create <仓库名称> --public --source=. --remote=origin
   ```
2. 推送 main 分支到 origin：
   ```bash
   git push -u origin main
   ```

完成后汇报仓库 URL 与推送结果。

### 阶段五：v0.1.0 Tag 与 Release

**本阶段执行前必须单独让用户确认是否执行，用户未明确同意则跳过。**

用户确认后，先检查项目是否适合发布 v0.1.0（功能基本可用、README 完整、无已知阻断性 Bug），然后准备以下内容：

1. **v0.1.0 Tag**
2. **Release 标题** — 格式建议：`v0.1.0 - <简短描述>`
3. **简洁的发布说明** — 概述本次发布的核心内容
4. **当前功能列表** — 列出已实现的功能

准备好后向用户展示以上内容，获得**最终确认**后执行：

1. 创建并推送 Tag：
   ```bash
   git tag -a v0.1.0 -m "Release v0.1.0"
   git push origin v0.1.0
   ```
2. 发布 GitHub Release：
   ```bash
   gh release create v0.1.0 --title "<Release标题>" --notes "<发布说明 + 当前功能列表>"
   ```

完成后汇报 Release URL。

---

## 四、确认门控汇总

| 阶段 | 确认要求 |
|------|----------|
| 一、完善 README | 无需确认，直接执行 |
| 二、敏感信息排查 | 只报告，不执行 Git；用户确认无问题后才进阶段三 |
| 三、初始化并首次提交 | 阶段二通过后自动执行 |
| 四、发布到 GitHub | 必须确认仓库名称、可见性（固定 Public）、操作清单 |
| 五、Tag 与 Release | 必须单独确认是否执行；执行前再确认 Release 内容 |

---

## 五、关键机制与硬约束

| 机制 | 具体要求 |
|------|----------|
| **README 不虚构** | 五章节内容必须与项目真实功能一致，不得编造 |
| **敏感排查与 Git 隔离** | 阶段二绝不执行任何 Git 操作，防止敏感信息被提交进历史 |
| **默认分支 main** | 首次提交前将分支重命名为 `main` |
| **可见性固定 Public** | 阶段四仓库固定为公开，不提供 Private 选项 |
| **阶段五可跳过** | Tag/Release 需用户单独确认，未同意则跳过，不强制发布 |
| **Release 双重确认** | 先确认是否执行，再确认具体 Release 内容，才真正执行 |
| **每阶段有汇报** | 完成后汇报关键结果（章节清单、提交哈希、仓库 URL、Release URL） |
| **顺序不可乱** | 五阶段必须按顺序执行，不得跳步 |

---

## 六、作用总结（一句话版）

| 作用点 | 具体表现 |
|--------|----------|
| **发布流程标准化** | 五阶段固定顺序，覆盖从 README 到 Release 的完整首次发布链路 |
| **敏感信息前置拦截** | 在 Git 操作之前扫描密码、隐私、绝对路径、临时文件，避免泄露进版本历史 |
| **README 质量兜底** | 强制五章节结构且不虚构，保证仓库开箱可读 |
| **用户全程可控** | 关键节点（敏感排查结果、仓库信息、是否发 Release、Release 内容）都需用户确认 |
| **首次提交规范** | 统一 `main` 分支、`Initial commit` 提交信息，符合社区惯例 |
| **版本发布规范** | 自动打 v0.1.0 Tag 并生成带标题、发布说明、功能列表的 GitHub Release |
| **操作可追溯** | 每阶段完成后汇报哈希、URL 等关键结果，用户清楚每一步发生了什么 |

**最终价值**：把"本地项目首次公开到 GitHub"这件容易漏步骤、容易泄露敏感信息、容易不规范的事，变成一条用户全程可见、可确认、可跳过的标准化发布流水线，确保最终交付的是一个 README 完整、历史干净、公开可访问、带正式版本号的 GitHub 仓库。
