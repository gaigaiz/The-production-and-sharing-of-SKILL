# Project-Execution-Planner Skill 作用总结

> 本总结基于 `project-execution-planner` Skill 的完整定义及其输出模板 `references/plan-template.md` 编写，涵盖其定位、适用场景、8 步工作流程、输出文档结构与关键约束，帮助使用者快速理解该 Skill 能做什么、怎么运转。

---

## 一、Skill 定位

**Project Execution Planner（项目执行计划生成器）** 是一套将**口语化项目需求**转化为**结构完整、可执行的项目执行计划 Markdown 文档**的标准化流程 Skill。

- **核心作用**：把"一段需求描述"拆解为带优先级、依赖关系、代码影响评估、风险校验、交付物清单和回滚方案的工程级执行计划。
- **一句话概括**：把需求翻译成"可落地、可验收、可回滚"的执行蓝图。
- **与 project-builder 的区别**：project-builder 负责"从零做项目"（含写代码和文档），本 Skill 只负责"做计划"，默认不生成完整代码。

---

## 二、适用场景与范围边界

| 维度 | 说明 |
|------|------|
| **触发场景** | "帮我做个项目计划"、"把需求拆成执行计划"、"评估代码变更影响"、"生成项目执行方案"、"拆解这个需求"等 |
| **适用输入** | 分步格式需求（`第 1 步：xxx；第 2 步：xxx`）或自由文本需求 |
| **核心能力** | 任务拆解与优先级排序、代码变更影响评估、风险与校验项识别、交付物清单、回滚方案制定 |
| **范围边界** | ① 仅用于任务拆解和代码变更影响评估；② **默认不直接生成完整项目代码**；③ 可根据用户额外指令补充关键代码片段（接口定义、核心逻辑伪代码），但不替代完整实现 |

---

## 三、核心工作流程：8 步串行

```
第1步 输入校验与规范化
   ↓
第2步 需求理解与冲突检测
   ↓
第3步 方案选定与备选记录
   ↓
第4步 任务拆解与依赖分析
   ↓
第5步 代码变更影响评估
   ↓
第6步 风险与校验项识别
   ↓
第7步 生成 Markdown 执行计划
   ↓
第8步 迭代更新（增量模式，用户后续修改需求时触发）
```

### 第 1 步：输入校验与规范化

检查用户输入是否为分步格式：

- **是分步格式** → 直接进入第 2 步。
- **非分步格式（自由文本）** → 向用户提示是否自动拆分；用户明确同意后，按语义边界拆分为编号步骤，并在文档「原始需求原文」中保留原始全文。

### 第 2 步：需求理解与冲突检测

逐条阅读需求，提取三类信息：

- **业务功能**：需要实现/修改的功能点
- **工程环境**：技术栈、框架、运行环境、依赖版本
- **代码基线**：现有代码结构、涉及模块、已有实现

**冲突检测**：若多条需求之间存在矛盾（如同时要求"保留旧接口"和"删除旧接口"），在文档中标明冲突项并向用户确认取舍，**不擅自取舍**。

### 第 3 步：方案选定与备选记录

若某需求存在多个可选实现方案：

- 默认选定一个方案写入执行计划，选择标准为：**对现有代码侵入最小、实现成本最低、与项目技术栈最匹配**；
- 在「工程前置约束」或对应任务备注中列出其余备选方案及简要说明；
- 用户后续指定更换方案时，重新评估代码影响并更新计划。

### 第 4 步：任务拆解与依赖分析

将每个需求步骤拆解为可执行子任务，每个子任务包含：

| 字段 | 说明 |
|------|------|
| 子任务名称 | 简洁描述要做什么 |
| 优先级 | P0（阻塞性，必须先完成）/ P1（核心功能）/ P2（增强项） |
| 依赖 | 前置子任务编号，无依赖则标注「无」 |
| 预估涉及文件/模块 | 可能需要修改的代码文件或模块 |

按依赖关系排序，确保**无循环依赖**。

### 第 5 步：代码变更影响评估

对每个涉及代码修改的子任务，评估：

- **修改范围**：新增 / 修改 / 删除
- **影响面**：该变更可能影响的其他模块或功能
- **兼容性**：是否破坏现有接口、数据结构或调用方

**代码修改原则（硬性约束）**：

1. 所有代码变更必须保证**不破坏原有项目代码功能**；
2. 当用户明确要求移除某项功能时，必须先检查待删除功能的代码依赖（被哪些模块调用、是否有共享逻辑），确认删除不会影响其余代码功能，并在计划中写明校验方式（如运行现有测试套件、检查调用方编译、执行回归用例）。

### 第 6 步：风险与校验项识别

识别三类风险，并为每个风险指定校验方式：

- **技术风险**：如第三方依赖兼容性、性能瓶颈
- **集成风险**：如模块间接口不一致、数据迁移
- **回归风险**：如修改可能影响已有功能

每个校验项需明确三要素：**校验内容、校验时机（开发中 / 提测前 / 上线前）、校验方法（自动化测试 / 手动验证 / 代码审查）**。

### 第 7 步：生成 Markdown 执行计划

按照 `references/plan-template.md` 固定章节模板生成文档，**8 个固定章节不可遗漏**（详见下一节）。

### 第 8 步：迭代更新（增量模式）

用户后续修改需求时：

1. 读取原有执行计划文档；
2. 识别变更点（新增 / 修改 / 删除需求）；
3. 重新评估受影响的子任务、依赖关系和代码影响；
4. 在文档末尾追加「版本变更记录」（版本号、变更日期、变更内容、影响范围）；
5. 输出更新后的完整文档，**不删除历史记录**。

---

## 四、输出文档结构（plan-template.md）

生成的执行计划文档包含固定头部和 8 个章节：

### 文档头部

```
# [项目名称] 执行计划
> 版本：v1.0 | 生成日期：YYYY-MM-DD | 状态：待确认/已确认/执行中
```

### 8 个固定章节

| 章节 | 核心内容 |
|------|----------|
| **1. 项目概述** | 2–3 句话概括项目目标、核心交付物和预期成果（不复述需求，提炼价值） |
| **2. 原始需求原文** | 逐字保留用户输入；自由文本自动拆分时同时保留原文和拆分结果；含「需求冲突项」表格（如有） |
| **3. 工程前置约束** | 技术栈与环境、选定方案与备选、其他约束；用户未提供的标注「待确认」，不编造 |
| **4. 任务拆解** | 任务总览表（编号/名称/优先级/依赖/涉及模块）+ 每个子任务详情（目标、操作步骤、涉及文件、验收标准） |
| **5. 代码变更规则** | 变更总览表（变更类型/范围/影响面/兼容性）+ 功能移除校验（如有）+ 通用原则 |
| **6. 风险与校验项** | 风险表（编号/描述/等级/关联任务/校验方式/校验时机）+ 关键校验清单 |
| **7. 交付物清单** | 交付物、类型、关联任务、交付标准（只列最终产物，不写中间过程） |
| **8. 回滚方案** | 回滚触发条件、回滚步骤（具体到可执行命令）、回滚责任人与时间预估、数据丢失风险 |

### 版本变更记录

文档末尾附版本变更记录表，迭代更新时追加新版本，不删除历史。

### 模板填写指引要点

- 子任务粒度以「一个人 1–3 天可完成」为宜；
- 优先级 P0 不超过总任务的 30%；
- 风险等级按「发生概率 × 影响程度」评定，每个风险必须有可执行的校验方式；
- 回滚方案必须具体到可执行的命令或操作，不能只写「回滚到上一版本」。

---

## 五、关键机制与硬性约束

| 机制 | 具体要求 |
|------|----------|
| **冲突不擅自取舍** | 需求矛盾时标明冲突项，交用户确认 |
| **方案选择标准** | 侵入最小、成本最低、与技术栈最匹配；备选方案留痕 |
| **代码不破坏原有功能** | 所有变更以不破坏现有功能为前提 |
| **功能移除必查依赖** | 删除前检查调用方、共享逻辑、数据依赖，并写明校验方式 |
| **子任务粒度控制** | 1–3 天可完成，P0 ≤ 30% |
| **风险必有校验** | 每个风险对应可执行的校验内容、时机、方法 |
| **回滚必须可执行** | 具体到命令/操作，含责任人、耗时、数据风险 |
| **增量可追溯** | 需求变更追加版本记录，不删历史 |
| **不编造信息** | 工程环境等用户未提供的标注「待确认」 |

---

## 六、作用总结（一句话版）

| 作用点 | 具体表现 |
|--------|----------|
| **需求结构化** | 自由文本也能拆成编号步骤，原文保留可追溯 |
| **冲突前置暴露** | 需求矛盾在动手前标出，避免做到一半返工 |
| **任务可执行** | 子任务带优先级、依赖、涉及模块、验收标准，粒度可控 |
| **变更有评估** | 每个代码改动都评估影响面和兼容性，删除功能必查依赖 |
| **风险有兜底** | 技术/集成/回归三类风险配校验方式和时机 |
| **交付物清晰** | 明确用户最终能拿到什么，不含中间过程 |
| **回滚可操作** | 触发条件、具体命令、责任人、耗时一应俱全 |
| **迭代可追溯** | 需求变更走增量模式，版本记录不删历史 |

**最终价值**：把"一段模糊的需求"转化为一份工程团队可直接据此排期、开发、测试、上线和回滚的执行计划文档，让项目在动手前就把依赖、风险、影响面和退路想清楚。
