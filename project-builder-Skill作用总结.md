# Project-Builder Skill 作用总结

> 本总结基于 `project-builder` Skill 的完整定义编写，涵盖其定位、适用场景、三阶段工作流、配套参考资产与状态管理机制，帮助使用者快速理解该 Skill 能做什么、怎么运转。

---

## 一、Skill 定位

**Project Builder（通用项目从零构建工作流）** 是一套面向"从零开始搭建项目"场景的标准化执行流程 Skill。它的作用不是代替用户写某一段代码，而是**把"一个模糊的项目想法"逐步变成"一个可运行、可交付、有文档的项目"**，全程用结构化流程约束 AI 的行为，避免需求不清就动手、做完没有说明文档等常见问题。

- **核心价值**：需求澄清 → 项目制作 → 文档生成 三阶段闭环，每阶段有明确规则和验收标准。
- **一句话概括**：先问清楚，再动手做，最后补文档。

---

## 二、适用场景与不适用场景

| 维度 | 说明 |
|------|------|
| **适用场景** | 用户提出项目想法但需求尚不明确，例如："帮我做一个 XX 项目/工具/脚本"、"我想开发一个 XX"、"从零开始做个 XX" |
| **不适用场景** | ① 需求已完全明确、只需写代码的简单任务；② 已有项目的修改与维护；③ 纯文档撰写 |

> 判断要点：**需求是否明确、是否从零开始**。两者同时满足才属于本 Skill 的工作范围。

---

## 三、核心工作机制：三阶段串行工作流

本 Skill 定义了三阶段串行工作流，**必须严格按顺序执行**，且每个阶段完成后需获得用户确认（或用户明确指令）才能进入下一阶段：

```
阶段一：需求澄清  →  阶段二：项目制作  →  阶段三：文档生成
```

### 阶段一：需求澄清

**启动条件**：用户提出项目想法或功能需求，但以下信息至少有一项不明确时进入本阶段：

1. 项目要解决的具体问题
2. 技术栈 / 编程语言
3. 运行平台
4. 输入输出形式

**提问规则**（约束 AI 的提问行为，保护用户体验）：

1. **一次只问一个问题**，不得一次抛出多个问题；
2. 从核心维度开始、按优先级逐步深入，提问维度参考 `references/clarification-questions.md`，按项目类型选择性覆盖，不逐条照搬；
3. 用户回答后判断信息是否充分：充分则进入下一维度或停止追问，不充分则继续深挖；
4. 技术选型等关键决策先询问用户偏好；用户表示不确定时，给出 1–2 个推荐方案并说明理由，由用户选择；
5. **不替用户编造**需求、预算、平台或验收标准。

**用户手动结束机制**：用户在任何时候说出以下任一表述，立即停止提问、不得继续追问：

- "够了"、"不用问了"、"问完了"、"可以了"
- "开始做吧"、"开始制作"、"动手吧"、"直接做"
- "进入下一步"、"下一个阶段"
- 或其他明确表示结束提问、开始制作的指令

**需求汇总确认**：停止提问后，先输出一份需求摘要（项目目标与背景、核心功能列表、技术栈与运行平台、输入输出形式、已明确的约束条件），再询问"以上需求是否准确？确认后开始制作"。用户确认后进入阶段二；用户提出修改则调整后再次确认。

### 阶段二：项目制作

**执行规范**：

1. 根据阶段一确认的需求，在用户指定目录（或当前工作目录）下创建项目文件；
2. **先规划项目结构**（目录、文件清单），告知用户后再开始编写代码；
3. 代码编写遵循对应语言的通用规范，添加必要注释；
4. 制作过程中遇到需求未覆盖的决策点，**暂停并询问用户**，不擅自做重大决定；
5. 关键功能完成后，如可运行则进行简单自测，确保基本功能正常。

**与用户沟通机制**：

- 制作开始时告知预计的文件结构和实现思路；
- 制作过程中定期同步进度（每完成一个主要模块）；
- 制作完成后，列出已创建的文件清单和实现的功能点，供用户确认。

### 阶段三：文档生成

**文档要求**：项目制作完成并经用户确认后，生成项目说明文档。文档命名为 `README.md`（如项目已有 README 则命名为 `项目说明.md`），放在项目根目录，必须严格按照**固定五段式结构**编写：

| 章节 | 内容 |
|------|------|
| ① 项目解决什么问题 | 项目背景与目标 |
| ② 主要功能 | 核心功能点列表 |
| ③ 安装方法 | 依赖安装与环境配置步骤 |
| ④ 使用方法 | 基本运行 / 调用方式 |
| ⑤ 输入输出示例 | 至少一个可运行的完整示例，包含输入和预期输出 |

**生成后验证**（AI 内部自检清单，不向用户展示）：

- 五个章节全部存在，无遗漏；
- 安装命令可直接复制运行；
- 使用示例包含真实可运行的命令；
- 输入输出示例完整，输入和预期输出都明确；
- 无 TODO、xxx 等占位符；信息确实不足时标注"待补充"并说明原因。

自检通过后，告知用户文档已生成，并简要说明文档位置和内容结构。

---

## 四、配套参考资产

Skill 目录下附带两份参考文档，为前两阶段提供细化指引：

### 1. `references/clarification-questions.md` — 需求澄清提问维度清单

将可提问的维度分层，AI 按项目类型**选择性提问**（仍遵守"一次一问"原则）：

- **核心维度（优先覆盖）**：
  1. 项目背景与目标（解决什么问题、使用人群、成功标准、现有方案差距）
  2. 功能范围（核心功能及优先级、第一版必做项、明确不做的边界）
  3. 技术栈与平台（语言/框架、运行平台、系统/API 对接、已有代码兼容）
  4. 输入与输出（输入形式、输出形式、具体格式要求）
  5. 依赖与环境（依赖安装、版本要求、环境变量/配置文件）

- **扩展维度（按需覆盖）**：
  6. 性能与规模（数据量、响应时间、并发/多用户）
  7. 错误处理与边界（异常输入处理、边界情况）
  8. 安全与权限（敏感数据、认证与权限控制）
  9. 交付形式（单脚本/可执行文件/完整目录/Docker 镜像、是否需打包分发）

- **提问原则**：一次一个问题；先核心后扩展；根据回答自适应（充分的维度不再追问，模糊的维度继续深挖）；不替用户做决定；用户可随时结束提问。

### 2. `references/doc-template.md` — 项目文档模板

为阶段三的五段式文档提供**详细写法示例与检查清单**：

- 每个章节必须填写、不得留空，信息不足时标注"待补充"并说明原因；
- 各章节均附"示例写法"（以批量图片压缩工具为例，给出完整的安装、用法、输入输出示例）；
- 附 8 项文档生成检查清单（章节齐全、功能点 ≥3、命令可直接运行、无占位符等）。

---

## 五、状态管理与中断恢复

- **阶段标注**：AI 每次回复开头用一句话标注当前所处阶段，例如 `[需求澄清阶段]`、`[项目制作阶段]`、`[文档生成阶段]`，用户始终清楚流程进度；
- **中断恢复**：会话中断后恢复时，AI 根据上下文判断当前阶段，主动告知用户并询问是否继续；
- **回到起点**：用户在任何阶段都可以说"回到需求澄清"、"重新确认需求"来回到阶段一。

---

## 六、作用总结（一句话版）

| 作用点 | 具体表现 |
|--------|----------|
| **控制提问节奏** | 一次一问、按维度递进、用户可随时叫停，避免需求沟通变成轰炸 |
| **保证需求真实** | 不编造需求/预算/平台/验收标准，关键决策先问用户、再给建议 |
| **规范制作流程** | 先规划结构再写码、遇决策点暂停询问、完成自测，杜绝边想边做 |
| **强制产出文档** | 五段式 README + 内部自检清单，保证每个项目都"可安装、可运行、可交付" |
| **流程透明可恢复** | 阶段标注 + 中断恢复 + 可随时回到需求澄清，全程用户可控 |

**最终价值**：把"从零构建项目"这一高风险、易跑偏的任务，转化为一个用户全程可见、可干预、可验收的三阶段标准化流程，确保最终交付物既有可运行的代码，也有能直接使用的说明文档。
